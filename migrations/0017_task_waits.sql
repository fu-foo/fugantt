-- タスクの待ち期間。
--
-- 「この日からこの日までは待ちなので数えない」を、1タスクにいくつでも。
-- 前の版は待ち理由・待ち先・待ち開始・想定解除という4つの列だったが、入力が
-- 重いわりに、いちばん欲しかった「日数から外す」に届いていなかった。
--
-- 独立した表にしないのは、この値がタスク以外から引かれることがないため。
-- `2026-08-17/2026-08-21` を空白区切りで並べる。
ALTER TABLE tasks ADD COLUMN waits TEXT NOT NULL DEFAULT '';
